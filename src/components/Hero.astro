---
import LinkedinSVG from "../assets/linkedin.svg";
import GithubSVG from "../assets/github.svg";
---

<section id="inicio" class="hero">
	<div class="info">
		<h1>Mark Sánchez García</h1>
		<span>Desarrollador Web</span>
		<p>
			Especializado en crear experiencias web modernas y atractivas con
			<code class="astro">Astro</code>,
			<code class="node">Node.js</code>, y las últimas tecnologías web.
		</p>
		<div class="buttons">
			<button class="primary">Proyectos</button>
			<button class="secondary">Contactar</button>
		</div>
	</div>

	<div class="redes">
		<ul>
			<li>
				<a
					href="https://www.linkedin.com/in/mark-s%C3%A1nchez-garc%C3%ADa-18bab1353/"
					target="_blank"
				>
					<LinkedinSVG />
				</a>
			</li>
			<li>
				<a href="https://github.com/MarkSG06" target="_blank">
					<GithubSVG />
				</a>
			</li>
		</ul>
	</div>
</section>

<style>
	.hero {
		color: white;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		text-align: center;
		gap: 2rem;
		height: 100vh;
		background:
			linear-gradient(rgba(16, 24, 40, 0.8), rgba(16, 24, 40, 0.9)),
			url(/src/assets/hero-background.svg) center/cover no-repeat;
		position: relative;
		overflow: hidden;
	}

	.info {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		max-width: 700px;
	}
	.info h1 {
		font-size: 3.5rem;
		font-weight: 700;
	}
	.info span {
		font-size: 1.8rem;
		font-weight: 500;
		color: #b24bab;
	}
	.info p {
		font-size: 1.1rem;
		line-height: 1.7rem;
		color: #d0d6e2;
	}
	.info code {
		background: rgba(255, 255, 255, 0.1);
		padding: 0.2rem 0.5rem;
		border-radius: 6px;
		font-weight: 700;
	}
	.info code.astro {
		color: #ff6209;
	}
	.info code.node {
		color: #75b957;
	}

	.buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		margin-top: 1rem;
	}
	.buttons button {
		cursor: pointer;
		padding: 0.8rem 1.6rem;
		font-size: 1rem;
		font-weight: 600;
		border-radius: 9999px;
		border: none;
		transition: all 0.3s ease;
	}
	.buttons .primary {
		background: #b24bab;
		color: white;
		box-shadow: 0 4px 12px rgba(178, 75, 171, 0.4);
	}
	.buttons .primary:hover {
		background: #9a3c94;
		transform: translateY(-2px);
	}
	.buttons .secondary {
		background: transparent;
		color: white;
		border: 2px solid white;
	}
	.buttons .secondary:hover {
		background: white;
		color: #101828;
	}

	.redes {
		position: absolute;
		left: 20px;
		top: 50%;
		transform: translateY(-50%);
	}
	.redes ul {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	.redes li a svg {
		width: 36px;
		height: 36px;
		fill: white;
		transition:
			transform 0.3s ease,
			fill 0.3s ease;
	}
	.redes li a:hover svg {
		fill: #b24bab;
		transform: scale(1.2);
	}

	@media (max-width: 768px) {
		.info h1 {
			font-size: 2.4rem;
		}
		.info span {
			font-size: 1.4rem;
		}
		.info p {
			font-size: 1rem;
		}
		.redes {
			display: none;
		}
		.redes li a svg {
			width: 28px;
			height: 28px;
		}
	}
</style>

<script></script>
