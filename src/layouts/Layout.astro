<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link
			href="https://fonts.googleapis.com/css2?family=Exo:wght@400;600;700&display=swap"
			rel="stylesheet"
		/>
		<title>Mark Sánchez - Curriculum</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	* {
		padding: 0;
		margin: 0;
		font-family: "Exo", sans-serif;
	}
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}

	:global(html) {
		height: 100%;
		overflow-y: scroll;
	}

	:global(::-webkit-scrollbar) {
		width: 12px;
	}

	:global(::-webkit-scrollbar-track) {
		background: linear-gradient(180deg, #0f0f0f, #1c1c1c);
	}

	:global(::-webkit-scrollbar-thumb) {
		background: linear-gradient(180deg, #ff0080, #7928ca, #2575fc);
		border-radius: 10px;
		background-size: 200% 200%;
		animation: scrollGradient 4s ease infinite;
		transition: all 0.3s ease;
	}

	:global(::-webkit-scrollbar-thumb:hover) {
		filter: brightness(1.3);
		transform: scaleX(1.1);
	}

	@keyframes scrollGradient {
		0% {
			background-position: 0% 50%;
		}
		50% {
			background-position: 100% 50%;
		}
		100% {
			background-position: 0% 50%;
		}
	}
</style>
